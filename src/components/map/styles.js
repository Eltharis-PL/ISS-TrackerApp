import { StyleSheet } from "react-native";

const mapStyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    mapView: {
        width: '100%',
        height: '50%',
    },
    issStyle: {
        width: 24,
        height: 28,
        tintColor: 'red',
    },
});

export default mapStyles;
