import { StyleSheet } from "react-native";

const mapStyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    mapView: {
        width: '100%',
        height: '50%',
    },
});

export default mapStyles;
